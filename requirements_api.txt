# Dependencias adicionales para la API REST (v2.0.0)
# Instalar junto a requirements.txt:
#   pip install -r requirements.txt -r requirements_api.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
